[project]
name = "rag-against-the-machine"
version = "0.1.0"
readme = "README.md"
requires-python = "==3.10.*"
dependencies = [
    "flake8",
    "flake8-pyproject",
    "mypy",
    "fire",
    "langchain",
    "langchain_community",
    "bm25s[core]",
    "ollama>=0.6.2",
]
